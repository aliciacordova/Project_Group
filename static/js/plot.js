d3 function select the text field
mybutton.on("submit", myfunction) where myfunction calls on the other functions
define myfuncion 
build a URL and the URL is the flask URL and endpoint to prvide info


// Create a function that is called directly from the
// index.html file when the crypto punk id is entered.
// The HTML will pass the argument 'this.value' (??) to the function
// which the function will manipulate as newSample within the
// function.
function optionChanged(newIdSelection) {
    // print the selected subject id to the console
    console.log(newIdSelection);

    // call on a function that will provide the demographics
    // metadata for the selected subject.
    buildTables(newIdSelection);
    
    // call on a function that will build the charts for the
    // selected subject.
    buildCharts(newIdSelection);
        
  }


// Create a function that will provide the relevant
// Crypto Punk data for the selected punk id.  
function buildTables(id_selection) {
    
     ///////////////////////////////////////////////////
    // Create a table with the rarity for all features
    ///////////////////////////////////////////////////



}


// Create a function that will build the charts for the
// selected subject.
function buildCharts(sample) {



    ///////////////////////////////////////////////////
    // Create the price chart
    ///////////////////////////////////////////////////





    ///////////////////////////////////////////////////
    // Create the network chart
    ///////////////////////////////////////////////////




}